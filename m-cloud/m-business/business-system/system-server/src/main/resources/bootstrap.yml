#======================================服务基础配置==============================start
spring:
  application:
    #服务名
    name: system-server
  profiles:
    active: dev

server:
  servlet:
    #服务前缀
    context-path: /
  #服务端口
  port: 8111
#======================================服务基础配置==============================end